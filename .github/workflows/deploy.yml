name: Build Hugo Site

# 触发条件
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# 权限配置
permissions:
  contents: read        # 读取仓库内容
  pull-requests: write  # PR评论
  statuses: write       # 更新commit状态
  pages: write          # 推送到gh-pages分支

# 环境变量
env:
  HUGO_VERSION: '0.111.3'
  HUGO_EXTENDED: true
  NODE_VERSION: '18'

# 任务定义
jobs:
  # 构建任务
  build:
    name: Build Hugo Site
    runs-on: ubuntu-latest

    steps:
      # 步骤1: 检出代码
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: false         # 不使用Git Submodule，改用Hugo Modules
          fetch-depth: 0            # 获取全部历史 (用于GitInfo)

      # 步骤1.5: 检查并创建gh-pages分支
      - name: Create gh-pages branch if not exists
        run: |
          # 设置Git用户信息
          git config --global user.email "action@github.com"
          git config --global user.name "github-actions[bot]"
          echo "🔍 检查gh-pages分支是否存在..."
          if git show-ref --verify --quiet refs/remotes/origin/gh-pages; then
            echo "✅ gh-pages分支已存在"
          else
            echo "ℹ️ gh-pages分支不存在，创建它..."
            # 创建一个空的gh-pages分支
            git checkout --orphan gh-pages
            git rm -rf . 2>/dev/null || true
            echo "# gh-pages" > README.md
            git add README.md
            git commit -m "Initial gh-pages branch"
            git push origin gh-pages
            echo "✅ gh-pages分支创建完成"
            # 切回main分支
            git checkout main
          fi

      # 步骤2: 设置Hugo
      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: ${{ env.HUGO_VERSION }}
          extended: ${{ env.HUGO_EXTENDED }}

      # 步骤2.5: 安装Hugo Modules依赖 ⭐ 关键变更
      - name: Install Hugo Modules
        run: |
          echo "🔄 初始化Hugo Modules..."
          # 启用Go模块
          export GO111MODULE=on
          # 使用hugo自动管理模块
          hugo mod tidy
          echo "✅ 模块配置完成"
          # 下载模块到本地缓存
          hugo mod download
          echo "✅ 模块缓存完成"

      # 步骤3: 设置Node.js (用于Stack主题SCSS编译)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          # 注意: 不使用cache，因为主题没有package-lock.json

      # 步骤3.5: 验证Hugo Modules
      - name: Verify Hugo Modules
        run: |
          echo "🔍 验证Hugo Modules配置..."
          cat go.mod
          echo ""
          echo "🔍 检查模块缓存..."
          hugo mod graph
          echo ""
          echo "🔍 验证主题存在..."
          ls -la $(go env GOPATH)/pkg/mod/github.com/caijimmy/ 2>/dev/null || echo "模块缓存位置未知"

      # 步骤4: 验证构建环境
      - name: Verify Build Environment
        run: |
          echo "🔍 环境验证..."
          echo "Hugo版本: $(hugo version)"
          echo "Node版本: $(node --version)"
          echo "npm版本: $(npm --version)"
          echo "主题目录: $(ls -la themes/stack/ | head -5)"
          echo "内容目录: $(ls -la content/ | head -5)"
          echo "assets目录: $(ls -la assets/ 2>/dev/null || echo '无assets目录')"
          echo "SCSS资源: $(ls -la assets/scss/ 2>/dev/null | head -3 || echo '无')"
          echo "TypeScript资源: $(ls -la assets/ts/ 2>/dev/null | head -3 || echo '无')"
          echo "SVG图标资源: $(ls -la assets/icons/ 2>/dev/null | head -3 || echo '无')"

      # 步骤5: 执行Hugo构建
      - name: Build Hugo Site
        run: |
          echo "🔨 开始构建Hugo站点..."
          hugo --gc --minify --verbose
          echo "✅ 构建完成"

      # 步骤7: 验证构建产物
      - name: Verify Build Output
        run: |
          echo "🔍 验证构建产物..."
          if [ -d "publish" ]; then
            echo "✅ publish目录存在"
            echo "文件数量: $(find publish -type f | wc -l)"
            echo "目录大小: $(du -sh publish | cut -f1)"
            echo "首页检查: $(test -f publish/index.html && echo '存在' || echo '缺失')"
            echo "Sitemap检查: $(test -f publish/sitemap.xml && echo '存在' || echo '缺失')"
            echo "RSS检查: $(test -f publish/index.xml && echo '存在' || echo '缺失')"
          else
            echo "❌ publish目录不存在!"
            exit 1
          fi

      # 步骤8: 推送构建产物到gh-pages分支 ⭐
      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v3
        if: github.ref == 'refs/heads/main'
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./publish
          publish_branch: gh-pages
          keep_files: true
          force_orphan: false

      # 步骤9: 添加PR评论 (仅PR时)
      - name: Comment PR with Build Status
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number
            });

            const repoUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}`;
            const ghPagesUrl = `https://${context.repo.owner}.github.io/${context.repo.repo}/`;
            const buildStatus = '${{ job.status }}';
            const commentBody = `📦 Hugo构建${buildStatus === 'success' ? '成功' : '失败'}!

            - **构建状态**: ${buildStatus.toUpperCase()}
            - **Commit**: ${context.sha.substring(0, 7)}
            - **构建者**: ${context.actor}
            - **时间**: ${new Date().toISOString()}
            - **预览链接**: [${ghPagesUrl}](${ghPagesUrl})
            - **gh-pages分支**: [查看代码](${repoUrl}/tree/gh-pages)

            > 4EVERLAND等部署平台可直接部署gh-pages分支的静态文件
            `;

            const botComment = comments.find(comment =>
              comment.user.type === 'Bot' && comment.body.includes('Hugo构建')
            );

            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: commentBody
              });
            } else {
              await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: commentBody
              });
            }

      # 步骤10: 更新Commit状态 (仅push到main时)
      - name: Update Commit Status
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/github-script@v7
        with:
          script: |
            const buildStatus = '${{ job.status }}';
            const state = buildStatus === 'success' ? 'success' : 'failure';
            const repoUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}`;
            const ghPagesUrl = `https://${context.repo.owner}.github.io/${context.repo.repo}/`;

            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: state,
              description: `Hugo构建${buildStatus === 'success' ? '成功，gh-pages已更新' : '失败'}`,
              context: 'Hugo Build',
              target_url: repoUrl
            });
